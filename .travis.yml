language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bower' ]; then cd .. && eval "mv $currentfolder generator-bower" && cd generator-bower; fi

